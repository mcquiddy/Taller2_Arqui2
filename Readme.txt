# COMANDOS PARA COMPILAR LOS ARCHIVOS

gcc fac_par.c -o fac_par -fopenmp -lm

gcc sax_par.c -o sax_par -fopenmp 

gcc sax.c -o sax -fopenmp 

gcc pi_par.c -o pi_par -fopenmp 

gcc pi.c -o pi -fopenmp
